{
  "name": "echo-agent",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Echo 的官方 CLI 与终端界面：唯一的 echo-agent 可执行文件（交互 / 管道两形态），以及把 Agent 事件流渲染成 transcript 的壳。",
  "bin": {
    "echo-agent": "./bin/echo-agent.ts"
  },
  "dependencies": {
    "@echo-agent/core": "workspace:*",
    "@earendil-works/pi-tui": "^0.84.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test test",
    "start": "bun bin/echo-agent.ts"
  },
  "exports": {
    ".": {
      "bun": "./src/index.ts"
    },
    "./package.json": "./package.json"
  }
}
