{
  "name": "@echo-agent/coding-agent",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Echo 的 coding agent preset：系统 prompt、权限策略与 echo:workspace / echo:shell 两条 Extension。装配归 createEcho()。",
  "dependencies": {
    "@echo-agent/core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test test"
  }
}
